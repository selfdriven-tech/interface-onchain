import { CborString } from "./CborString/index.js";
import { Cloneable } from "./utils/Cloneable.js";
export interface ISubCborRef {
    _bytes: Uint8Array;
    start: number;
    end: number;
}
export declare class SubCborRef extends CborString implements ISubCborRef, Cloneable<SubCborRef> {
    _bytes: Uint8Array;
    start: number;
    end: number;
    constructor({ _bytes, start, end }: ISubCborRef);
    toString(): string;
    toBuffer(): Uint8Array;
    clone(): SubCborRef;
}
